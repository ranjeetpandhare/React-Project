import React from 'react'

function FunctionCompWithProperty(props) {
    return (
        <div>
            <h1> hi empid is {props.empid} and empname is {props.empname}</h1>
        </div>
    )
}
export default FunctionCompWithProperty

